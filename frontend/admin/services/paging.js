import angular from 'angular';

class Paging {
  constructor($resource) {

  }

  pagination(num) {
    
  }
}



export default angular.module('services.paging', [])
  .service('paging', Paging)
  .name;

GetArticles['$inject'] = ['$resource'];
